---
apiVersion: v1
kind: Service
metadata:
  name: be-realworld
  labels:
    app: realworld
    type: backend
spec:
  selector:
    app: realworld
    type: backend
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}